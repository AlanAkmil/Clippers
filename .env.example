# AI transcript + viral-scoring feature (Groq)
# Get a free key at https://console.groq.com/keys — no credit card needed.
# NEVER commit your real key. Set this in your hosting dashboard
# (Vercel → Project Settings → Environment Variables, or Cloudflare → Settings → Variables),
# then redeploy.
GROQ_API_KEY=
